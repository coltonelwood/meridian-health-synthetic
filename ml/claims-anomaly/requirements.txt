# Claims anomaly detection dependencies
# Maintained by @mrodriguez

scikit-learn==1.3.2
pandas==2.1.4
numpy==1.26.2
tensorflow==2.15.0  # for autoencoder (experimental)
mlflow==2.9.2
pyarrow==14.0.1
matplotlib==3.8.2
seaborn==0.13.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# imbalanced-learn==0.11.0  # tried SMOTE, didn't help for anomaly detection
# hdbscan==0.8.33  # clustering approach, abandoned

# NOTE: tensorflow is heavy, we might switch to pytorch if the
# autoencoder approach pans out. TF is ~1.5GB installed.
